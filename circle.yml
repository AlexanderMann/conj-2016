dependencies:
  cache_directories:
    - /home/ubuntu/.cache/pip
    - /home/ubuntu/.cache/wheelhouse
  pre:
    - pip --version
    - pip install wheel
    - pip wheel --wheel-dir=/home/ubuntu/.cache/wheelhouse numpy
    - pip wheel --wheel-dir=/home/ubuntu/.cache/wheelhouse matplotlib
    - pip install --user --find-links=/home/ubuntu/.cache/wheelhouse numpy matplotlib:
        timeout: 1200
